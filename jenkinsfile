pipeline {
    agent {
        label "SLAVE"
    }
    stages {
        stage('seeing artifacts') {
            steps {
                sh '''
                zip login.zip *
                '''
            }
        }
    }
}